class Over {
    constructor(runCount, totalRunCount, wicketCount, overCount, totalBallCount, extras, statusButtonID) {
                    this.runCount = runCount;
                    this.totalRunCount = totalRunCount;
                    this.wicketCount = wicketCount;
                    this.overCount = overCount;
                    this.totalBallCount = totalBallCount;
                    this.extras = extras;
                    this.statusButtonID = statusButtonID;
                }
}

export default Over;